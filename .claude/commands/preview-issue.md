Open a newsletter HTML file in the default browser.

File to open: $ARGUMENTS
(If blank, list the available .html files in the project root and ask which one to open.)

- Windows : run `start <absolute-path>`
- macOS   : run `open <absolute-path>`
- Linux   : run `xdg-open <absolute-path>`

Use the absolute path to the file.
After the command runs, tell the user which file was opened and its absolute path.
